name: Sitemap Generator

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Sitemap is a Next.js Metadata Route
        run: |
          echo "Sitemap is served by app/sitemap.ts at /sitemap.xml."
          echo "Do not write public/sitemap.xml — it would override the complete route."
