import ForgeContactPage from '../../../components/forge/ForgeContactPage';

export const metadata = { title: 'Contact — Quantiva Forge' };

export default function Page() {
  return <ForgeContactPage locale="de" />;
}
