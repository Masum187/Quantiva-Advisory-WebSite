import ForgeTeamPage from '../../../components/forge/ForgeTeamPage';

export const metadata = { title: 'Team — Quantiva Forge' };

export default function Page() {
  return <ForgeTeamPage locale="en" />;
}
