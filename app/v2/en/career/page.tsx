import ForgeCareerPage from '../../../components/forge/ForgeCareerPage';

export const metadata = { title: 'Careers — Quantiva Forge' };

export default function Page() {
  return <ForgeCareerPage locale="en" />;
}
